package Strings;
import java.util.*;
public class equalsto {
    public static void main(String[] args){
        String s1="hello";
        String s2="hello";
        String s4=new String("Dark");
        String s3=new String("hello");
        System.out.println(s2.equals(s3));
        System.out.println(s4.equals(s2));

    }
}
//.equals  is used to compare two strings that they are same or not for s2.equals(s3) here s2 get comapred by s3 both are hello so its true